name: crafti_hub
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  another_flushbar: ^1.12.30
  carousel_slider: ^5.0.0
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3
  flutter_svprogresshud: ^1.1.0
  google_fonts: ^6.2.1
  image_picker: ^1.1.2
  intl: ^0.20.2
  lucide_icons: ^0.257.0
  path: ^1.9.0
  provider: ^6.1.2
  shared_preferences: ^2.5.2
  sizer: ^3.0.5
  flutter_svprogresshud: ^1.1.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/image1/

flutter_launcher_icons:
  image_path: "assets/logo.jpg"
  android: "launcher_icon"
  ios: true
  min_sdk_android: 24
  adaptive_icon_background: "#ffffff" # You can use a color or image for the background
  adaptive_icon_foreground: "assets/logo.jpg" # For adaptive icons on Android
